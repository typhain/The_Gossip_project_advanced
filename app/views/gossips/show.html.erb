
<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
      <h1 class="display-4">Mate ton gossip</h1>
      <p class="lead">Toutes les les infos les plus croustillantes</p>
    </div>

<div class="container"
<div class="card mb-4 box-shadow">
          <div class="card-header">
            <h4 class="my-0 font-weight-normal d-flex w-100 justify-content-center">Détails du gossip</h4>
          </div>
          <div class="card-body"
            <ul class="list-unstyled mt-3 mb-4 d-flex w-100 justify-content-center">
              <p class="card-text"><%= link_to "#{@gossip.user.first_name}", user_path(@gossip.user.id) %></p>
              <p class="card-text"><%= @gossip.title %></p>
              <p class="card-text"><em><%= @gossip.content %><em></p>
              <p class="card-text"><%= link_to "#{@gossip.user.city.name}", city_path(@gossip.user.city.id) %></p>
            </ul>
            <div class="card-body d-flex w-100 justify-content-xl-center">
              <a <%= link_to "Edit", edit_gossip_path(@gossip.id), :class => "btn btn-outline-primary" %> </a>
              <a <%= link_to "Delete", gossip_path(@gossip.id), :class => "btn btn-outline-danger", :method => :delete%> </a>
            </div>
          </div>
        </div>

  <div class="container">
    <div class="card mb-4 box-shadow">
      <%= form_for @gossip do |f| %>
        <div class="form-group">
          <%= f.text_field :content, class: 'form-control' %>
        </div>
        <div class="form-group d-flex w-100 justify-content-xl-center">
          <button type="submit"class="btn btn-success  <%= link_to "Comment", edit_gossip_path(@gossip.id), :class => "btn btn-outline-success" %> </button>
      <% end %>
    </div>
  </div>
